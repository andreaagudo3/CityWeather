platform :ios, '9.0'

target 'cityWeather' do
    
    use_frameworks!
    pod 'Alamofire'
    pod 'ObjectMapper', '~> 2.0.0'
    
    target 'cityWeatherTests' do
        inherit! :search_paths
        
    end
    
    target 'cityWeatherUITests' do
        inherit! :search_paths
        # Pods for testing
    end
    
end
